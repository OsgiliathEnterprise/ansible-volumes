# Tasks: fix nested mount point creation for LVM filesystems

References: proposal.md (why/what), specs/volume-mountpoints/spec.md (behavior contract), design.md D1–D4 (how). Assumptions recorded here per design Open Questions: scenario LV is `nfs_nested` (512M — mkfs.xfs requires >300MB, so the initial 256M assumption was raised during implementation; xfs, mntp `/var/nfs/volume`, no explicit `mount` key) and all three scenarios (`default`, `parallels`, `kvm`) are updated identically.

## 1. Role fix (tasks/lv.yml)

- [x] 1.1 Add the mount point directory pre-creation task to `tasks/lv.yml` immediately before the `include_role: mrlesmithjr.manage_lvm` block, per design D1 (`ansible.builtin.file`, `state: directory`, `path: "{{ avlv.mntp }}"`, `become: true`, gated on `avlv.mntp is defined and avlv.mntp != 'None'`, task name following the file's `Lv | ...` convention). Verify: `yamllint tasks/lv.yml` passes and the task appears before the include in the rendered order.
- [x] 1.2 Replace `mount: "{{ avlv.mount | default(false) }}"` in the same `include_role` vars with the explicit-wins / computed-default expression from design D2 (explicit `avlv.mount` always wins; otherwise true iff `mntp` is defined/non-`None` and `filesystem` defaults to a real fs, i.e. not `None`/`swap`). Verify: `yamllint tasks/lv.yml` passes, and for each LV shape in the design D2 table the rendered value matches (spot-check by rendering the expression with an ad-hoc `ansible -m debug` or molecule converge output).
- [x] 1.3 Run the lint cycle per AGENTS.md (`uv tool run --python 3.13 --with tox tox -e lint`; fall back to local `tox.ini` envs if monorepo env names are unavailable) and fix any findings in the touched files. Verify: lint exits 0 with no new violations in `tasks/lv.yml`.

## 2. Molecule scenario + tests

- [x] 2.1 Add the `nfs_nested` LV (size 512M — raised from an initial 256M assumption because mkfs.xfs requires >300MB; create true, filesystem xfs, mntp `/var/nfs/volume`, **no** explicit `mount` key) to `molecule/default/converge.yml`, `molecule/parallels/converge.yml`, and `molecule/kvm/converge.yml`, placed after `nfs_exposed` in each `lvmetanames` list (design D3; kvm has no `nfs_exposed`, so it is appended at the end of its list). Verify: `yamllint` passes on all three files and the LV appears after `nfs_exposed` in every file.
- [x] 2.2 Add testinfra tests to `molecule/default/tests/test_default.py` (shared by all scenarios via symlink) per design D4, following the file's existing style: nested mount point directory exists (`sudo test -d /var/nfs/volume`), mounted as xfs at that path (`mountpoint -q` and/or `xfs_info /dev/non-persistent/nfs_nested | grep -c 'ftype=1'`, mirroring `test_formating_is_xfs`), and writable (write + read back a file). Verify: `flake8 molecule/default/tests/test_default.py` passes and the new tests reference `/var/nfs/volume`.

## 3. Run until success (AGENTS.md test workflow)

- [x] 3.1 Destroy + converge on the local scenario per AGENTS.md (`uv tool run --python 3.13 --with tox tox -e destroy -- --scenario-name=parallels`, then `tox -e converge-monorepo -- --scenario-name=parallels`; fall back to local `tox.ini` envs `destroy`/`converge` if monorepo env names are unavailable). Verify: converge completes with zero failed tasks — specifically no `xfs_growfs: path resolution failed for /var/nfs/volume` and the `Lv | ensure mountpoint directory exists` task runs before the manage_lvm include.
- [x] 3.2 Run idempotence per AGENTS.md (`tox -e idempotence-monorepo -- --scenario-name=parallels`, or local `idempotence` env). Verify: second converge reports no changes for the directory pre-creation task and no failed tasks overall. (Note: one spurious `changed` from community.general.lvol on thinpoolmeta's 10%VG size — pre-existing %VG rounding quirk, all subsequent passes clean; not caused by this change.)
- [x] 3.3 Run verify per AGENTS.md (`tox -e verify-monorepo -- --scenario-name=parallels`, or local `verify` env) with the new tests from 2.2. Verify: all testinfra tests pass, including the three nested-mount-point assertions. (Result: 13/13 passed, incl. test_nested_mountpoint_directory_exists, test_nested_xfs_volume_is_mounted, test_nested_xfs_formatting_is_xfs, test_nested_mountpoint_is_writable.)
- [x] 3.4 Spot-check sibling roles' `lvm_groups` definitions that consume this role (`tcharl.nfs_server`, `tcharl.ansible_containerization`, `tcharl.ansible_virtualization`, and other consumers found via grep) for LVs with a real filesystem + mntp but no explicit `mount` key, per the design risk note. Verify: findings recorded in the change's discovery notes (expected outcome: such LVs will now auto-mount; flag any consumer that genuinely needs `mount: false`). (Done — see discovery-notes.md: only tcharl.ansible_orchestration's nfs_exposed is affected, beneficially; no consumer needs mount:false.)
